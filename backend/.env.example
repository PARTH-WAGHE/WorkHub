# === MySQL (AlwaysData WorkHub) ===
DB_HOST=mysql-workhub.alwaysdata.net
DB_PORT=3306
DB_NAME=workhub_db
DB_USER=workhub_app
DB_PASSWORD=WorkHubApp_S3cure!2025
HIBERNATE_DIALECT=org.hibernate.dialect.MariaDBDialect

# Spring Boot will also honor a single JDBC URL if you prefer:
# SPRING_DATASOURCE_URL=jdbc:mariadb://mysql-workhub.alwaysdata.net:3306/workhub_db?sslMode=REQUIRED

# Render/Cloud port (Spring uses server.port=${PORT:8080})
# PORT=8080
