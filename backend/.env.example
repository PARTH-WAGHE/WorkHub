# === PostgreSQL (WorkHub) ===
DB_HOST=localhost
DB_PORT=5432
DB_NAME=workhub_db
DB_USER=workhub_app
DB_PASSWORD=ChangeMe_S3cure!
DB_SSL_MODE=disable
HIBERNATE_DIALECT=org.hibernate.dialect.PostgreSQLDialect

# Spring Boot will also honor a single JDBC URL if you prefer:
# SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/workhub_db?sslmode=require

# Render/Cloud port (Spring uses server.port=${PORT:8080})
# PORT=8080
