services:
  - type: web
    name: queuefree
    env: node
    plan: free
    buildCommand: npm run build
    startCommand: npm start
    publishDir: build
    envVars:
      - key: NODE_ENV
        value: production
